<configuration>

    <conversionRule conversionWord="callerUserId" converterClass="skills.TomcatConfig$UserIdConverter"/>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <Pattern>access.skills-service [%callerUserId] %h %l %u [%t] "%r" %s %b %D "%i{Referer}" "%i{User-Agent} %q"</Pattern>
        </encoder>
    </appender>

    <appender-ref ref="STDOUT" />

</configuration>